stages:
  - deploy

deploy_job:
  stage: deploy
  tags:
    - deploy
  script:
    - echo "Deployment starting..."
    - whoami
    - pwd
    - rsync -av --exclude='.git' ./ /mnt/general_accounting/
    - echo "Done"